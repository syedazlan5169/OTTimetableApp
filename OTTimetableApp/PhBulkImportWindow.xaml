<Window x:Class="OTTimetableApp.PhBulkImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Public Holiday Bulk Import" Height="520" Width="680"
        ResizeMode="CanResize" WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Calendar selector -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Calendar:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <ComboBox x:Name="CalendarFilter" Width="240"
                      DisplayMemberPath="Display" SelectedValuePath="Id"
                      SelectionChanged="CalendarFilter_SelectionChanged"/>
        </StackPanel>

        <!-- File browser -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,4">
            <Button Content="Browse CSV..." Width="110" Click="Browse_Click" Margin="0,0,8,0"/>
            <TextBlock x:Name="FilePathText" VerticalAlignment="Center"
                       Foreground="Gray" Text="No file selected" TextTrimming="CharacterEllipsis" MaxWidth="480"/>
        </StackPanel>

        <!-- Format hint -->
        <TextBlock Grid.Row="2" Margin="0,0,0,8" FontSize="11" Foreground="Gray"
                   Text="Expected CSV format — one entry per line: date,Holiday Name  (e.g. 01/01/2026,New Year's Day)  — header row is auto-detected and skipped."/>

        <!-- Preview DataGrid -->
        <DataGrid x:Name="PreviewGrid" Grid.Row="3"
                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsValid}" Value="False">
                            <Setter Property="Foreground" Value="Red"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date"         Binding="{Binding DateDisplay}" Width="100"/>
                <DataGridTextColumn Header="Holiday Name" Binding="{Binding Name}"        Width="*"/>
                <DataGridTextColumn Header="Status"       Binding="{Binding Status}"      Width="210"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Row summary -->
        <TextBlock x:Name="StatusLabel" Grid.Row="4" Margin="0,6,0,0" Foreground="Gray" FontSize="11"/>

        <!-- Footer -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button x:Name="ImportButton" Content="Import" Width="120" IsEnabled="False"
                    Margin="0,0,10,0" Click="Import_Click"/>
            <Button Content="Close" Width="80" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
