<Window x:Class="OTTimetableApp.AuditLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Change Log" Height="520" Width="860" ResizeMode="CanResize" WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Filter bar -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Calendar:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <ComboBox x:Name="CalendarFilter" Width="200" DisplayMemberPath="Display"
                      SelectedValuePath="Id" SelectionChanged="Filter_Changed" Margin="0,0,12,0"/>

            <TextBlock Text="Action:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <ComboBox x:Name="ActionFilter" Width="160" SelectionChanged="Filter_Changed" Margin="0,0,12,0"/>

            <Button Content="Refresh" Width="80" Click="Refresh_Click"/>
        </StackPanel>

        <!-- Log grid -->
        <DataGrid x:Name="LogGrid" Grid.Row="1"
                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp, StringFormat=\{0:dd/MM/yyyy HH:mm:ss\}}" Width="150"/>
                <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="140"/>
                <DataGridTextColumn Header="Calendar" Binding="{Binding CalendarName}" Width="130"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <TextBlock x:Name="CountLabel" VerticalAlignment="Center" Margin="0,0,12,0" Foreground="Gray"/>
            <Button Content="Close" Width="80" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
