<Window x:Class="OTTimetableApp.ReplacementReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Monthly Replacement Report" Height="520" Width="860"
        ResizeMode="CanResize" WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Filter bar -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Calendar:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <ComboBox x:Name="CalendarFilter" Width="200"
                      DisplayMemberPath="Display" SelectedValuePath="Id"
                      Margin="0,0,16,0"/>

            <TextBlock Text="Month:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <ComboBox x:Name="MonthFilter" Width="130"
                      DisplayMemberPath="Name" SelectedValuePath="Month"
                      Margin="0,0,16,0"/>

            <Button Content="Load" Width="80" Click="Load_Click"/>
        </StackPanel>

        <!-- Report grid -->
        <DataGrid x:Name="ReportGrid" Grid.Row="1"
                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date"       Binding="{Binding DateDisplay}"  Width="100"/>
                <DataGridTextColumn Header="Day"        Binding="{Binding DayName}"      Width="100"/>
                <DataGridTextColumn Header="Shift"      Binding="{Binding Shift}"        Width="160"/>
                <DataGridTextColumn Header="Group"      Binding="{Binding GroupName}"    Width="90"/>
                <DataGridTextColumn Header="Slot #"     Binding="{Binding SlotIndex}"    Width="60"/>
                <DataGridTextColumn Header="Replaced By" Binding="{Binding ReplacedBy}" Width="*"/>
                <DataGridTextColumn Header="Replaces"   Binding="{Binding Replaces}"     Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <TextBlock x:Name="CountLabel" VerticalAlignment="Center" Margin="0,0,12,0" Foreground="Gray"/>
            <Button Content="Close" Width="80" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
