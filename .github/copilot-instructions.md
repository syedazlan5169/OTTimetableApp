# Copilot Instructions

## Project Guidelines
- In OT calculation, `BaseGroupId` is not a reliable source of truth; determine employee group membership/shift ownership via `GroupMembers` by `EmployeeId` (and/or shift slot assignment) instead.